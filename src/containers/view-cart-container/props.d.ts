export interface ITabIconProps {
    isActived: boolean;
}

export interface IBasketTabProps {
    handleTabChange: any;
}

export interface IToggleCheckedItemProps {
    code: string;
    isChecked: boolean;
}