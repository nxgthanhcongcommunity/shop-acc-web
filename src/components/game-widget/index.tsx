import { Section, SubSlider } from "..";

const GameWidget = (props: any) => {
  return (
    <Section title="Nick đột kích" seeMoreUrl="zbc">
      <SubSlider.Style2 />
    </Section>
  );
};
export default GameWidget;
